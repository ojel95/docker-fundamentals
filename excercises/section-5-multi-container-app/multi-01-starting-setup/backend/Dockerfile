FROM node:14

WORKDIR /app

COPY package.json .

RUN npm install

COPY . .

# ARG DEFAULT_PORT=80

# ENV PORT $DEFAULT_PORT

EXPOSE 80

ENV MONGODB_USERNAME=root
ENV MONGODB_PASSWORD=1234

CMD [ "npm", "start" ]